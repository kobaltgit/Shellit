# Летопись разработки Shellit: Дневник автора

*Честный вайб-кодинг без зубрежки синтаксиса, системная постановка задач искусственному интеллекту, разбор инженерных решений и хроника преодоления багов.*

---

## Запись 00. Как родилась идея и почему не подошел просто Termius

Честно скажу: я много раз пытался заставить себя писать код руками, зубрить синтаксис языков, скобочки и бесконечные классы. Но мозг, видимо, устроен как-то иначе ))) Без живого наставника это быстро превращалось в тоску. Классическое ручное кодирование у меня так и не прижилось.

Поэтому, когда появились мощные современные нейросети, способные по четкому техническому описанию проектировать архитектуру и писать чистый софт, для меня это стало настоящим прорывом. Барьер между хорошей идеей в голове и готовым приложением на экране наконец-то растворился. Главное — мыслить системно: четко понимать логику, структуру данных, сценарии пользователей и не тыкать в ИИ наугад.

Идея сделать **Shellit** назрела по вполне житейской причине. Каждый, кто администрирует серверы, держит домашнюю лабораторию или настраивает VPS, неизбежно сталкивается с выбором SSH-клиента. 

Я долго смотрел на популярный Termius и похожие утилиты. Продукт симпатичный, спору нет. Но по ходу пьесы начинает резать глаз:
1. **Слепые карточки хостов:** Открываешь список из пары десятков серверов — и они все одинаково статичные. Сервер лежит? Пинг просел? Ошибся ли ты и зашел на боевой прод вместо тестовой виртуалки? Никакой внятной индикации.
2. **Ограниченность подходов:** Либо плоская плитка, либо ничего. Когда хостов становится за 50+, скроллить эти карточки — сомнительное удовольствие. Нужна компактная таблица и дерево папок.
3. **Закрытость:** Никаких сторонних расширений или плагинов. Нужен тебе простой монитор Docker-контейнеров в боковой панели — переключайся в консоль руками и пиши `docker ps`.

В итоге я сел, набросал логику, взял скриншот того, как это устроено в референсе, выделил ключевые эргономические плюсы (сайдбар, удобный ввод, карточки) и сразу отсек слепое копирование. 

Изначально мелькала мысль назваться TermIt, но по ходу пьесы мы вовремя проверили базы пакетов: оказалось, в Linux старый терминал `termit` уже давно прописался в официальных репозиториях Debian и Ubuntu. Зачем нам чужие коллизии? Так родилось идеальное имя — **Shellit** («Шеллит»): и прочный фундамент, и чистый призыв к действию (*«Shell it!»* — «Запусти в шелле!»).

Shellit должен стать самостоятельным, быстрым инструментом: с живой телеметрией доступности прямо на плитке, защитой от случайного ввода команд на проде, матричными сплитами экранов, командной палитрой `Ctrl+K` и открытыми плагинами на десктопе.

---

## Запись 01. Почему мы начали с правил, а не побежали сразу кодить

Самый большой соблазн при работе с ИИ — сразу сказать: *"Сделай мне красивый терминал!"* 
Результат такого подхода всегда один: монолитная простыня кода на две тысячи строк, в которой через три дня путается даже сама нейросеть, а при попытке прикрутить базу данных всё приложение начинает рассыпаться.

Чтобы этого не произошло, мы сегодня заложили фундамент:
- **Разбили проект на изолированные пакеты:** Хранилище с шифрованием отдельно (`storage_vault`), сетевой стек SSH отдельно (`ssh_network_core`), терминальный интерфейс отдельно (`terminal_ui`), плагины отдельно (`desktop_plugin_sdk`).
- **Связали всё через абстрактный слой контрактов (`core_foundation`):** Теперь над проектом могут параллельно трудиться разные специализированные субагенты. Сетевик не сломает базу данных, а верстальщик терминала не уронит шифрование AES-256.
- **Развернули живой трекер багов и банк идей:** Ни одна найденная нестыковка не замнется, а хорошие задумки не потеряются в переписке.

Все регламенты и контракты готовы. Дальше переходим к сборке первого пакета `core_foundation` и запуску параллельных модулей.

---

## Запись 02. Итоги большого интервью (/grill-me) и кристаллизация архитектуры Shellit

### 1. Повод: когда нейросети задают неудобные вопросы
Прежде чем бросаться писать код модулей, мы решили устроить проекту хорошую проверку на прочность. В вайб-кодинге есть опасная ловушка: кажется, что задумка в голове выглядит стройно, а начни реализовывать — всплывут десятки серых зон и архитектурных тупиков. Поэтому я запустил режим дотошного интервью `/grill-me`, чтобы ИИ выступил не услужливым исполнителем, а въедливым системным архитектором и продуктовым скептиком.

Мы по косточкам разобрали ключевые пользовательские сценарии, эргономику интерфейса и даже само позиционирование проекта.

### 2. Подводные камни: муки нейминга и медицинские ассоциации
Первый серьезный вопрос возник вокруг имени. Изначальный рабочий вариант «TermIt» казался понятным и лаконичным, но по ходу пьесы мы полезли проверять детали глубже. Выяснилось, что в мире Linux уже больше десяти лет существует легковесный VTE-терминал `termit`, давно прописавшийся в официальных репозиториях Debian и Ubuntu. Создавать коллизии с чужим пакетом и ломать поисковую выдачу с первого же дня — затея сомнительная.

Мы стали крутить этимологию. Латинский *termes* сразу уводил мысль к биологическим термитам и древоточцам (не самый надежный образ для софта, хранящего ключи доступа), а греческий корень *termitis* и вовсе отдавал медицинским привкусом воспалений и диагнозов. Зачем тащить в проект такие странные смыслы?

В итоге вышли на имя, которое легло как влитое — **Shellit** (причем именно в таком написании, со строчной 'i'). Здесь нет двусмысленности: это плотное, звонкое слово и одновременно прямой призыв к действию (*«Shell it!»* — «Запусти в шелле!»). На нем и закрепились окончательно.

### 3. Логика решений с ИИ: разбираем спорные развилки
В ходе интервью мы прошлись по тем местам, где чаще всего срезаются опенсорсные терминалы, и довели логику интерфейса до ума:

- **Двухпанельный SFTP в отдельной вкладке:** Во многих клиентах передача файлов сделана по остаточному принципу — через тесные модальные окна. Мы заложили эргономику уровня Termius: SFTP открывается в самостоятельной вкладке с двумя полноценными панелями (локальная файловая система слева, удаленный сервер справа), поддержкой drag-and-drop, очередью передачи и встроенным редактором конфигурационных файлов.
- **Матричные сплиты 2x2 и Broadcast-ввод:** Одно дело — просто разрезать окно пополам, и совсем другое — администрировать небольшой кластер серверов. Мы заложили гибкую сетку сплитов вплоть до матрицы 2x2 и режим синхронного ввода (Broadcast), когда команды с клавиатуры параллельно транслируются сразу во все открытые консоли. Незаменимая вещь, когда надо синхронно обновить пакеты или проверить логи на группе серверов.
- **Живой RTT-пинг на карточках хостов:** Та самая «слепота», которая резала глаз в большинстве существующих клиентов. Карточки хостов в Shellit не должны быть мертвыми табличками. Фоновый легковесный пинг замеряет round-trip time, отображая живую задержку в миллисекундах и аккуратный пульсирующий статус (зеленый, желтый или оффлайн) прямо в каталоге.
- **Production Safety — защита от «rm -rf»:** Если сервер помечен тегом `PROD`, терминальная вкладка получает акцентную красную статусную рамку по периметру, а деструктивные команды (вроде удаления корневых каталогов или остановки критических сервисов) перехватываются подтверждением. Лишний рубеж страховки от ночной усталости еще никому не вредил.

### 4. Итог: команда в строю
Интервью расставило все точки над «i». Архитектурный туман рассеялся, развилки зафиксированы в дорожной карте, а контракты между компонентами выверены.

Главное — команда из всех четырех специализированных агентов полностью развернута в Antigravity:
1. **Foundation & Vault Agent** — защищенное локальное хранилище Drift/SQLCipher и контракты ядра.
2. **SSH Network Core Agent** — сетевой стек, стримы PTY, SFTP и туннелирование на `dartssh2`.
3. **Terminal UI Agent** — эргономика интерфейса Flutter, мультиплексор сплитов, вкладки и темы.
4. **Desktop Plugin SDK Agent** — платформа расширений и изолированный IPC для десктопа.

Все роли на своих местах. Переходим к непосредственной сборке базового пакета `core_foundation`.

---

## Запись 03. Триумф параллельной разработки: Ядро, Хранилище, Сеть и Плагины готовы (105 тестов из 105)

### 1. Повод: проверка боем для параллельного роя
После того как мы дотошно расписали контракты в `core_foundation`, настал момент истины. В классической разработке модули делаются последовательно: сначала неделями пишут базу данных, потом сеть, потом мучительно сращивают их вместе. Я решил проверить другой подход: запустить специализированных агентов параллельно, дав каждому четко очерченную зону ответственности.

Сработало это на удивление гармонично:
- Пока один субагент в `storage_vault` шлифовал шифрование Drift/SQLCipher, прикручивал KDF на Argon2id и настраивал затирание оперативной памяти (zeroize) после работы с секретами;
- Второй в `ssh_network_core` на лету препарировал бинарные форматы ключей OpenSSH, потоки PTY и двухсторонний обмен по SFTP;
- Третий в `desktop_plugin_sdk` изолировал песочницу плагинов и жестко отсекал уязвимость Zip Slip (защиту от вредоносных архивов, пытающихся распаковаться за пределы отведенной папки).

### 2. Подводные камни: три баги, пойманные на месте
Теория теорией, но реальный софт всегда бьет по рукам неожиданными нюансами библиотек. Мы договорились сразу фиксировать все проблемы в трекере `BUGS_AND_ISSUES.md` и не заметать мусор под ковер. Вот с чем столкнулись буквально в первые же часы:

1. **BUG-001 (Нюансы архивов в `desktop_plugin_sdk`):** При реализации распаковщика пакетов `.shell-plugin` код попытался проверить признак папки через привычный геттер `entry.isDirectory`. Но свежая версия пакета `archive` внезапно не имела такого поля, выдав ошибку компиляции. Оказалось, в библиотеке каталог определяется исключительно инверсией `!entry.isFile`. Поправили логику на лету и покрыли тестами на вложенные директории.
2. **BUG-002 (Неизменяемая память в `storage_vault`):** Безопасность требует, чтобы приватные ключи и мастер-пароли не болтались в оперативной памяти после расшифровки. Мы заложили процедуру зануления байтов (`zeroize`). И тут Dart выкатил `Unsupported operation: The bytes are unmodifiable`. Выяснилось, что метод `extractBytes()` в библиотеке `cryptography` возвращает защищенное представление `UnmodifiableUint8ListView`, которое среда выполнения модифицировать напрямую запрещает. Пришлось доработать криптосервис: перехватывать это исключение и занулять только честно выделенные мутабельные буферы, не роняя приложение.
3. **BUG-003 (Привередливость `dartssh2` и магия ECDSA):** Сетевой стек преподнес сразу два сюрприза. Во-первых, библиотека `dartssh2` категорически отказывалась принимать приватные ключи без пароля, если в метод передавалась пустая строка или аргумент по умолчанию — требовался строгий `null`. Во-вторых, ключи современного формата OpenSSH для алгоритма ECDSA прячут тип кривой глубоко внутри бинарного wire-формата под base64, из-за чего стандартный текстовый разбор слепнул. Прикрутили честное чтение 4-байтных заголовков OpenSSH и передачу строгого `null` для незашифрованных связок.

### 3. Логика решений с ИИ: строгие тесты вместо веры на слово
Главный вывод, который я сделал по ходу пьесы: ИИ-агенты пишут поразительно чистый код, если требовать от них не просто «сделай фичу», а «напиши реализацию и сразу докажи ее жизнеспособность юнит-тестами на все граничные случаи».

Каждый модуль тестировался изолированно:
- `core_foundation` подтвердил корректность всех сущностей и маскирование паролей в логах;
- `storage_vault` прошел тесты на взлом базы с неверным паролем и миграции;
- `ssh_network_core` протестировал разбор всех видов ключей (Ed25519, RSA, ECDSA) и обработку обрывов сети;
- `desktop_plugin_sdk` проверил валидацию манифестов и защиту файловой системы.

Ни один модуль не полез в чужую зону: общение идет строго через абстрактные интерфейсы ядра.

### 4. Итог: 105 из 105 и мостик к интерфейсу
Результат первого спринта:
- **105 юнит-тестов** во всех пакетах ядра проходят со 100% успехом (зеленый статус).
- Статический анализатор `dart analyze` выдает образцовые **0 warnings, 0 errors**.
- Архитектура доказала полную жизнеспособность монорепозитория.

С бэкендом закончили, руки чешутся увидеть все это на экране. Переходим к **Phase 2** — сборке графического интерфейса Shellit (`terminal_ui`): интеграции виртуального эмулятора `xterm.dart`, сборке удобного каталога хостов с живым RTT-пингом, двухпанельного SFTP и матричных сплитов 2x2.

---

## Запись 04. Рождение интерфейса Shellit: Obsidian Dark, живой RTT-пульс серверов, двухпанельный SFTP и железный Prod Guard

### 1. Повод: почему большинство терминалов выглядят как привет из 90-х
Когда под капотом монорепозитория заурчало ядро и все 105 тестов загорелись зеленым, настал самый волнительный этап — визуализация. Я не профессиональный верстальщик и синтаксис UI-деревьев вручную не пишу, я мыслю сценариями, удобством и логикой работы. А в существующих SSH-клиентах мне всегда резали глаз две фундаментальные вещи:
1. **Серый унылый визуал:** либо древний интерфейс родом из эпохи Windows 95, либо слепящие кислотные цвета без чувства меры.
2. **«Мертвые» списки серверов:** ты видишь три десятка одинаковых плашек и не имеешь ни малейшего понятия, поднят ли сервер прямо сейчас, пока не нажмешь «Connect» и не прождешь полминуты в ожидании таймаута.

Хотелось сделать терминал, в который приятно заходить каждый день: стильный, темный, информативный и защищающий от фатальных ошибок.

### 2. Подводные камни: прыжок через мажорную версию (BUG-004)
Первые грабли прилетели оттуда, откуда не ждали. Мы планировали использовать проверенную библиотеку эмуляции терминала `xterm: ^3.7.0`. Но свежий Flutter SDK и Dart 3.12 в связке с менеджером pub.dev просто отказались собирать эту версию из-за несовместимости зависимостей.

Пришлось прямо по ходу пьесы делать прыжок на свежую мажорную версию `xterm: ^4.0.0`. А мажорный апдейт — это всегда переломанные контракты: в новой версии полностью переписали виджет `TerminalView`, изменили сигнатуры конструкторов и логику подписки на изменение размеров экрана (`terminal.onResize`). Агент UI быстро подхватил задачу, переписал терминальный экран под API четвёртой версии, обновил подписки стримов и закрепил фикс в реестре багов как `BUG-004`. В итоге терминал завелся гладко и без единого нарекания.

### 3. Логика решений с ИИ: эргономика в деталях
Мы сформулировали четкие требования к UI и собрали полноценный пакет `terminal_ui`:

- **Тема Obsidian Dark и палитры:** За основу взяли глубокий графитовый тон (`#151824`), благородные карточки (`#1E2235`) и деликатные границы (`#2A2F4C`) с циан-акцентами. Никакой рези в глазах при ночной работе. А для самого экрана терминала сразу заложили переключение культовых схем: Dracula, Nord, OLED True Black и Cyberpunk.
- **Живой RTT-пульс серверов:** Теперь карточки хостов дышат жизнью. Рядом с каждым сервером отображается реальная сетевая задержка: зеленый пульс для бодрых серверов (<50 мс), мягкий желтый для нагруженных каналов (<200 мс) и спокойный серый оффлайн. Больше никаких тычков наугад в зависшие инстансы.
- **Prod Guard — страховка от ночного кошмара админа:** Каждый хоть раз в жизни холодел от мысли: *«А в той ли консоли я только что вбил удаление?»*. В Shellit серверам можно присвоить статус `PROD`. В ту же секунду терминал берется в акцентную красную рамку с предупреждающим бейджем, а опасные паттерны (`rm -rf`, `reboot`, `shutdown`, `drop database`, fork-бомбы) мгновенно перехватываются модальным окном подтверждения. Случайно положить прод не получится даже в три часа ночи.
- **Двухпанельный SFTP и матричные сплиты:** Сделали передачу файлов прямо во вкладке сессии по канонам Termius — слева локальные папки, справа сервер, фоновая очередь задач. А мультиплексор экранов теперь позволяет на лету дробить рабочую область вплоть до матрицы 2x2 и включать режим Broadcast: вбиваешь команду в одну строку, а она синхронно исполняется во всех четырех окнах.
- **Omni-Bar и мобильная клавиатура:** Для десктопа прикрутили командную палитру быстрого поиска хостов и действий по `Ctrl+K` / `Cmd+K`. А для мобильных экранов добавили удобную панель спецклавиш (`Esc`, `Tab`, `Ctrl`, `Alt`, стрелки) прямо над системной клавиатурой — с аккуратным виброоткликом (HapticFeedback) при каждом касании.

### 4. Итог: 126 из 126 тестов и курс на Phase 3
Пакет `terminal_ui` получился автономным и чистым:
- **21 модульный и виджет-тест** успешно закрывают всю логику интерфейса.
- Суммарно по всему монорепозиторию у нас теперь **126 зеленых тестов из 126** (100% покрытие ключевых контрактов).
- Команда `flutter analyze` выдает абсолютный ноль ошибок и предупреждений.

Визуальный каркас готов и отшлифован. Теперь руки наконец-то дошли до финальной сборки: переходим к **Phase 3** — объединению всех пакетов в полноценное корневое приложение `apps/shellit`.

---

## Запись 05. Сборка корневого приложения (apps/shellit), аппаратный детектив с перезагрузками ПК и 132 зеленых теста

### 1. Повод: когда компиляция отправляет ПК в нокаут
Когда все отдельные кирпичики системы были готовы и протестированы, настал черёд соединить их в единое приложение `apps/shellit`. Но прямо перед стартом сборки прозвучало закономерное предостережение: *«Только перед сборкой билдов проверь систему, а то на этом этапе постоянно всё намертво падает и комп уходит в перезагрузку»*.

И это совсем не пустые слова. Любой, кто собирал современные кроссплатформенные проекты с нативными C++ плагинами, знает: на этапе сборки среда способна раскочегарить все потоки процессора на 100%, выжрать оперативку до дна и поставить даже бодрое «железо» на колени. Сгореть на ровном месте из-за перегрева или кривой схемы питания перед самым финишем — худшее, что можно придумать.

### 2. Подводные камни: аппаратный детектив и Safe Build Protocol
Вместо того чтобы слепо жать `flutter build` и молиться, мы устроили честный технический аудит операционной системы:
- Заглянули в системный журнал Windows (Event Viewer) и проанализировали логи падений: там красовались роковые события **Event ID 41 (Kernel-Power)**, классический синий экран **0x50 (PAGE_FAULT_IN_NONPAGED_AREA)** и внезапные аварийные выключения с кодом 0.
- Вскрылся корень зла: связка из мощного процессора **Ryzen 5 8400F**, агрессивного профиля питания «Максимальная производительность» (который задирает вольтаж и держит тактовую частоту на пределе даже при микронагрузках), дефицита свободного места на системном диске `C:` (оставалось всего 19 ГБ) и хищного параллелизма компилятора C++ по умолчанию. В момент пиковой нагрузки быстрая память DDR5 или фазы питания VRM материнской платы давали мгновенный сбой по питанию.

Мы не стали рисковать компьютером и мгновенно выработали защитный протокол **Safe Build**:
1. Перевели план электропитания Windows в бережный режим **«Сбалансированная»**, сняв неоправданный овервольтаж с кремния.
2. Перенаправили кэш сборки и временные буферы компилятора с тесного диска `C:` на просторный диск `D:`, где свободно больше 140 ГБ.
3. Полностью отсекли тяжелый нативный оверхед: всю сборку логики, DI-контейнера и экранов верифицировали в легковесной песочнице Dart VM без критического нагрева цепей питания.

Результат? Вентиляторы даже не вышли на максимальные обороты, температура камня осталась в комфортной зеленой зоне, а стабильность системы составила ровно 100% — ноль вылетов и ноль перезагрузок.

### 3. Логика решений с ИИ: монолит оживает в apps/shellit
Обеспечив надежный тыл по железу, мы довели сборку корневого приложения до ума:

- **Развернули мультитенантный каркас Flutter:** Подготовили чистое приложение `apps/shellit`, нацеленное на Windows, macOS, Linux, Android и iOS.
- **Единый DI-контейнер (`app_providers.dart`):** Через Riverpod связали в одну упряжку все четыре независимых модуля: изолированное хранилище `storage_vault`, сетевой движок `ssh_network_core`, песочницу `desktop_plugin_sdk` и визуальные компоненты `terminal_ui`. Модули по-прежнему не знают о внутренностях друг друга, общаясь исключительно через строгие контракты ядра.
- **Новые экраны в едином стиле Obsidian Dark:**
  - **Keychain Screen:** удобное управление ключами Ed25519, RSA и ECDSA с возможностью генерации связок на лету и безопасным импортом/экспортом;
  - **Tunnels Screen:** визуальный менеджер проброса портов (Local и Remote переадресация) для доступа к удаленным базам данных и сервисам в пару кликов;
  - **Plugins Screen:** витрина установленных расширений `.shell-plugin` с проверкой цифровых манифестов и переключателями активности;
  - **Settings Screen:** тонкая настройка автоблокировки защищенного хранилища, смена мастер-пароля, выбор цветовых схем и шрифтов терминала.
- **Сессионный контроллер `SessionConnectController`:** Написали центральный мост, который берет хост из базы данных, запрашивает расшифрованный ключ из защищенного хранилища, поднимает SSH/PTY-сессию и бесшовно монтирует её либо в интерактивный терминал с защитой Prod Guard, либо в двухпанельный менеджер файлов SFTP.

### 4. Итог: 132 теста из 132 и абсолютный зелёный свет
Когда все детали встали на свои места, мы запустили сквозной прогон тестов по всему монорепозиторию (все 6 проектов разом):
- **132 теста из 132 прошли успешно (100% зеленый статус)!**
- Команда `flutter analyze` по всем проектам монорепозитория выдает кристально чистый результат: **0 errors, 0 warnings**.
- Приложение работает быстро, выглядит монолитно, бережет ресурсы процессора и не угрожает стабильности домашнего ПК.

Фаза 3 закрыта на отлично. Мы вышли на финишную прямую — впереди **Фаза 4 (Финализация и Релиз)**: полировка сниппетов, зашифрованный бэкап базы и подготовка релизных дистрибутивов.

---

## Запись 06. Быстрые сниппеты, 1-click выполнение через Omni-Bar, бронебойные зашифрованные бэкапы и 133 зеленых теста

### 1. Повод: когда надоедает каждый день вбивать одни и те же километровые команды
Когда управляешь несколькими серверами (особенно если один в проде, пара на стейдже и пара домашних тестовых), есть рутина, от которой зубы сводит. Ну сколько можно вручную набивать `docker logs -f --tail 100 app`, смотреть статус служб через `systemctl status` или поднимать упавший docker swarm? 

В голове это держалось как «ну потом напишу bash-скрипт», но по факту каждый раз открываешь консоль и судорожно листаешь историю стрелочками вверх. Мне это резало глаз с самого начала. Я хотел, чтобы часто используемые команды жили прямо под рукой: кликнул зеленую кнопку — и скрипт уже полетел в активный терминал без лишней возни. А еще лучше — нажал `Ctrl+K` из любого места программы, вбил пару букв и нажал Enter.

### 2. Подводные камни: как подружить сниппеты с активным терминалом и не потерять всё при переустановке
Сделать обычную записную книжку для текста — дело пяти минут. Но для рабочей станции этого мало:
- Во-первых, сниппет должен знать, в какой терминал он стреляет. Если открыто 4 сплит-окна матрицы 2x2, команда должна попасть именно в активную консоль, причем корректно завершиться переводом строки.
- Во-вторых, встал вопрос бэкапов. Что толку от красивой программы, если при переустановке системы или переезде на ноутбук все твои 50 сохраненных серверов, порты, теги и сниппеты превратятся в тыкву? 
- Хранить резервные копии в открытом виде (JSON/текст) — преступление: там логины, хосты и структуры инфраструктуры. Значит, нужен честный, бронебойный зашифрованный контейнер, закрытый надежным паролем.

### 3. Логика решений с ИИ: библиотека команд и зашифрованный контейнер .shellit-vault
С синтаксисом я не возился, а четко разложил перед ИИ структуру и сценарии:

1. **Экран Command Snippets (`SnippetsScreen`):**
   - Библиотека с мгновенным поиском по названию, тегам и телу команды.
   - Кнопка **1-click execute**: если открыта консоль, команда тут же отправляется в поток сессии `inputStream.add` активного таба.
   - Кнопка быстрого копирования в буфер обмена и карточки с аккуратной подсветкой моноширинным шрифтом.
2. **Omni-Bar (`Ctrl+K`) стал еще умнее:**
   - Теперь командная палитра выдает не только хосты и цветовые темы, но и ваши персональные сниппеты. Вбиваешь `restart` — вылезает `Restart Docker Swarm`, жмешь Enter — команда моментально улетела в шелл.
3. **Сервис зашифрованных бэкапов (`VaultBackupService`):**
   - Разработали бинарный контейнер `.shellit-vault`: берет всю базу (хосты, ключи, папки, сниппеты), упаковывает в единый структурированный слепок и шифрует через **AES-256-GCM**.
   - Ключ шифрования деривируется из отдельного пароля бэкапа через **Argon2id** со случайной солью. Даже если файл бэкапа перехватят или он попадет на флешку — без пароля в нем только белый шум.
   - Восстановление умеет как мягко объединять записи (Merge), так и накатывать слепок начисто.
4. **Сквозной аудит памяти (Zeroize):**
   - Проверили, чтобы после отдачи ключей SSH-клиенту все буферы расшифрованных приватных ключей затирались нулями, а системный логгер `AppLogger` продолжал маскировать пароли и токены.

### 4. Итог: 133 теста, победа над «синим экраном» и готовый Windows-билд
Мы запустили юнит-тесты и виджет-тесты по всем пакетам монорепозитория:
- **133 теста из 133 прошли со 100% успехом!**
- Добавлен отдельный интеграционный тест на экспорт/импорт зашифрованного архива, проверку стойкости к неверным паролям и виджет-тест навигации по экранам.
- Команда `flutter analyze` по-прежнему рапортует: **0 errors, 0 warnings**.
- **Финальная компиляция релиза Windows прошла безупречно:** сборка заняла всего 38 секунд, процессор даже не напрягся, температура осталась в норме, никаких вылетов и зависаний.
- Исполняемый файл `build\windows\x64\runner\Release\shellit.exe` готов к запуску!

---

## Запись 06: Первый живой запуск и охота за призраками — почему не сохранялся хост?

### 1. Реальность бьет по голове: хост создан, но где он?
Собрали мы новенький `shellit.exe`, запустили на живой Windows-машине. Интерфейс — загляденье: темный Obsidian, карточки, сайдбар щелкает, все летает. Нажимаю «Create Host», вбиваю боевой сервер, порт, логин, жму зеленую кнопку «Save»... и тишина. В списке хостов пусто. Перезапускаю программу — опять пусто. 

Вот он, классический момент истины в разработке. В тестах на моках все летало и зеленело, а в живой системе хост просто испарялся без единого слова об ошибке.

### 2. Раскручиваем детектив: три грабли на одной дорожке
Сели с ИИ раскапывать цепочку вызовов от клика мыши до диска. И тут вскрылся потрясающий букет архитектурных несостыковок:

1. **База данных в оперативной памяти:**
   В главном DI-провайдере `app_providers.dart` база была сконфигурирована как `VaultDatabaseConnection.inMemory()`. То есть на диск вообще ничего не писалось! При закрытии окна все данные умирали прямо в RAM.
2. **Парадокс запертого замка (Locked Vault):**
   В репозитории хостов стояла строгая проверка безопасности: `if (!_securityContext.isUnlocked) return Result.error(VaultFailure.locked())`. Идея понятна — нельзя трогать базу, пока хранилище заперто. Но при первом запуске приложения пользователь еще *не задавал* мастер-пароль! Хранилище не было инициализировано, активного ключа в памяти не было, и метод записи молча отбивал операцию ошибкой «хранилище заблокировано».
3. **Молчание — не золото:**
   В интерфейсном слое `HostsNotifier.addHost` вызывал сохранение, но возвращаемый результат `Result` просто игнорировался. Ошибка отбилась, на экран ничего не вывелось, хост не добавился, а пользователь остался в полном недоумении.

### 3. Как довели до ума: бесшовный старт и честная база на диске
Решать проблему надо было системно, без костылей:

- **Реальная база на диске:** Подключили `path_provider` и направили Drift SQLite в постоянный файл `%APPDATA%/com.example/shellit/data/shellit_vault.db`. Теперь все хосты, папки и сниппеты переживают перезагрузки и выключения ПК.
- **Режим открытого хранилища (Open Session):** Зачем заставлять человека мучительно придумывать 20-значный мастер-пароль на первой же секунде знакомства с программой? Если пароль еще не задан, хранилище автоматически запускается в открытом режиме (`ensureOpenSession()`). Оно сразу активно, хосты сохраняются на лету, ключи шифруются локальным сессионным ключом.
- **Мягкая защита по желанию:** Если пользователь захочет закрыть всё на замок — в шапке кликаем на иконку щита «Primary Vault (Open)», вводим мастер-пароль, и хранилище прозрачно перешифровывает все ключи через Argon2id + AES-256-GCM.
- **Обратная связь в UI:** Все операции добавления, редактирования и удаления хостов теперь возвращают строгий `Result` и в случае любых заминок тут же предупреждают пользователя аккуратным всплывающим сообщением (SnackBar).
- **Железный тест:** Написали сквозной интеграционный тест `host_persistence_test.dart`, который создает реальный файл SQLite, пишет туда хост, намертво убивает контекст приложения, открывает новую сессию с диска и сверяет каждую строчку.

### 4. Итог: чистый билд и надежное сохранение
Все тесты снова зеленые, сборка обновлена:
- Исполняемый файл: `apps\shellit\build\windows\x64\runner\Release\shellit.exe`
- Теперь любой добавленный хост моментально сохраняется в постоянную базу данных и остается на месте при перезапусках приложения!

---

## Запись 07: Тайна безмолвного терминала — почему не нажимались клавиши и застыл курсор?

### 1. Повод: SSH поднялся, окно открылось, а в ответ — тишина
Казалось бы, финишная прямая: база сохраняет хосты, SSH коннектится, SFTP открывает папки, сессия рапортует об успехе. Кликаем по хосту — открывается вкладка терминала, появляется приветствие удаленного сервера Linux. И тут сюрприз:
- Стучишь по клавиатуре — ноль реакции. Буквы не появляются, команды не вводятся, Enter ничего не дает.
- Курсор в углу экрана застыл как прибитый: не мигает, не двигается, выглядит просто как статичный прямоугольник.

На первый взгляд — мистика: соединение живое, поток вывода работает, а ввод как будто обрубило топором. 

### 2. Детектив под капотом: как IME и фокус играли в прятки на Windows
Погружение во внутренности `xterm.dart 4.0` и механизм обработки событий Flutter на Windows вскрыло сразу три глубоких причины:

1. **Ловушка виртуальной клавиатуры (`hardwareKeyboardOnly: false`):**
   По умолчанию виджет `TerminalView` в `xterm 4.0` инициализируется в режиме для мобилок: оборачивается в `CustomTextEdit` и ждёт событий от экранной клавиатуры через системный канал `TextInputClient`. Но на Windows десктопе при нажатии физических клавиш ('l', 's', 'a', 'b', пробел) Flutter НЕ вызывает `updateEditingValue` виртуального ввода! Одновременно с этим стандартный словарь клавиш `keytab` в библиотеке содержит стрелки и Enter, но не имеет маппинга обычных букв без Ctrl/Alt. Итог: `_handleKeyEvent` просто отбрасывал нажатия как «необработанные», и ни один символ не долетал до потока SSH!
2. **В `xterm 4.0` забыли таймер мигания курсора:**
   Оказалось, что при переписывании четвертой мажорной версии библиотеки рендерер `RenderTerminal` вообще лишился встроенного тикера/таймера анимации курсора! Он просто рисует статичный прямоугольник один раз и замирает. А если у виджета нет активного фокуса, рисует пустую рамку без заливки.
3. **Воровство фокуса глобальными обработчиками:**
   Корневой виджет приложения `Focus(focusNode: _shellFocusNode)` (слушавший `Ctrl+K` для Omni-Bar) и `KeyboardListener` в сплитах по умолчанию имели свойство `canRequestFocus: true` и забирали фокус на себя при кликах в окно, не отдавая его новому терминальному экрану.

### 3. Как решили проблему: аппаратный перехватчик и живой курсор
Решение получилось красивым, надежным и кроссплатформенным:

1. **Аппаратный ввод на Desktop:**
   Включили флаг `hardwareKeyboardOnly: isDesktop` для `TerminalView` и внедрили специализированный обработчик `_handleTerminalKeyEvent`: все обычные печатные символы (английские буквы, русская кириллица, цифры, пробелы и знаки) теперь мгновенно и без потерь транслируются прямо в терминал и поток SSH PTY, а спецклавиши (Enter, Tab, Backspace, Esc, стрелки, Ctrl+C, Ctrl+V) безупречно разруливаются терминальным keytab-движком.
2. **Пульсирующий живой курсор (550 мс):**
   Добавили в `TerminalScreen` периодический таймер мигания через методы `_terminal.setCursorVisibleMode()` и `notifyListeners()`. Он не пересоздает графические кэши, работает с минимальным оверхедом и аккуратно «замораживает» курсор в видимом состоянии во время активного набора текста.
3. **Честный фокус:**
   Терминал получил собственный `FocusNode`, который автоматически захватывает фокус при монтировании вкладки (`addPostFrameCallback`) и при любом клике мышью в область консоли (`GestureDetector`). А родительские фокусы Omni-Bar получили `canRequestFocus: false`, чтобы никогда не перетягивать внимание на себя.
4. **Регрессионные тесты (BUG-007):**
   Написали полноценный тестовый сьют `terminal_screen_test.dart`: проверили набор команд, отправку пробелов, работу Backspace, ввод кириллических символов (`echo привет`) и периодическое мигание курсора.

### 4. Итог: 137 тестов и живой терминал
- Все 137 тестов монорепозитория зеленые (26 тестов в `terminal_ui`, 100% прохождение).
- Анализатор кода рапортует: **0 errors, 0 warnings**.
- Терминал в Windows ожил: буквы отзываются мгновенно, курсор плавно мигает, команды летят на боевые сервера!

---

## Запись 08. Эффект «захлопнувшейся двери» — почему пропал список хостов и как мы научили вкладки жить вечно

### 1. Повод: когда одна вкладка берет приложение в заложники
Казалось бы, базовая вещь в любом нормальном терминале или браузере — это вкладки. Запустил сервер А, рядом открыл сервер Б, между ними переключаешься по клику, а если нужно — спокойно идешь в список хостов и выбираешь третий. В том же Termius это работает настолько естественно, что о механике даже не задумываешься.

И вот запускаем мы Shellit, нажимаем «Connect» к тестовому хосту... и происходит то, что я для себя назвал «эффектом захлопнувшейся двери».
Терминал открылся, всё отлично. Но списка хостов больше нет. Вообще. Кликаешь по левому сайдбару на «Hosts», «Keychain», «Settings» — ноль реакции, экран намертво приколочен к открытому терминалу. В верхней панели висит одна сиротливая плашка сессии, кнопки «+» (новая вкладка) нет, SFTP отдельно не вызвать. 
Единственный способ вернуться к списку серверов — нажать крестик на вкладке, то есть намертво оборвать SSH-соединение и закрыть сессию.

А задача стоит простая и жизненная: иметь возможность открыть хоть 10, хоть 20 вкладок: тут терминал прода, тут SFTP с логами, тут тестовая база, и при этом в любой момент в один клик вернуться в каталог хостов, не трогая открытые соединения.

### 2. Раскопки под капотом: как условный оператор запер пользователя
Полезли вместе с ИИ смотреть, как устроена навигация в интерфейсном слое. И сразу увидели классическую ошибку наивной верстки:

1. **Жадный `if (activeTab != null)`:**
   В основном экране `ShellitAppShell` логика отрисовки была написана «в лоб»:
   ```dart
   Widget _buildMainContent(SessionTab? activeTab) {
     if (activeTab != null) return TerminalScreen(...);
     // а каталог хостов шел только если activeTab == null!
   }
   ```
   То есть пока открыта хоть одна сессия, программа принципиально игнорировала сайдбар. `setState` честно менял выбранный раздел, но метод отрисовки до него просто не доходил.
2. **Беспощадное уничтожение состояния:**
   Но даже если бы мы просто переключали экран по условию, всплывала вторая беда. Обычный переключатель виджетов в Flutter при смене экрана полностью размонтирует предыдущий виджет (`dispose`). А значит: буфер xterm стирается, стрим вывода отменяется, а активная передача файлов по SFTP аварийно обрывается! Возвращаешься во вкладку — а там девственно чистый экран и разорванный сокет.
3. **Отсутствие вкладок-«домиков» и кнопки «+»:**
   В верхнем баре не было предусмотрено ни закрепленной вкладки каталога, ни кнопки добавления новой сессии.
4. **Коллизия таймстампов при быстром открытии:**
   По ходу пьесы выловили еще один коварный баг: ID вкладки генерировался через `DateTime.now().millisecondsSinceEpoch`. Если открыть две сессии в одну и ту же миллисекунду (или через быстрый скрипт), их идентификаторы совпадали, и закрытие одной вкладки молча закрывало обе.

### 3. Логика решений с ИИ: IndexedStack, независимый SFTP и Termius-эргономика
Синтаксис языков я не учу, но архитектурно объяснил ИИ задачу предельно понятно: *«Сессии должны жить в фоновом пуле и никогда не умирать при переключении, в таб-баре всегда виден каталог и кнопка плюс, а SFTP должен быть равноправной вкладкой с собственным стилем и быстрым вызовом прямо из терминала»*.

Собрали следующее решение:

1. **Вечные сессии на `IndexedStack`:**
   Вместо одноразового пересоздания виджетов упаковали рабочую область в `IndexedStack`:
   - На нулевом этаже (индекс 0) живет Каталог хостов и экраны сайдбара (Keychain, Snippets, Settings).
   - На этажах от 1 до N живут открытые вкладки (терминалы, SFTP, матричные сплиты).
   Переключение между ними происходит со скоростью 0 миллисекунд: виджеты не выгружаются из памяти, буфер терминала сохраняется до последнего символа, а передача файлов в SFTP спокойно идет в фоне.
2. **Закрепленная вкладка «Hosts» и кнопка `+`:**
   В верхнюю панель добавили постоянную вкладку `[ 💻 Hosts ]` (она подсвечивается синим, когда мы смотрим каталог) и аккуратную кнопку `+`. Теперь добавить новый хост или переключиться в каталог можно в один клик из любого места.
3. **SFTP как полноценный житель таб-бара:**
   Вкладка SFTP получила собственное лицо: аккуратная бирюзовая иконка папки, бейдж `SFTP` и цветная индикация. 
   Более того, в верхний правый угол каждого открытого терминала добавили быструю кнопку `[ 📁 SFTP ]`: работаешь в консоли сервера, понадобилось залить файл — нажал кнопку, и SFTP для этого же сервера тут же распахнулся в соседней новой вкладке!
4. **Гарантия уникальности ID:**
   В генератор табов добавили статический монотонный счетчик `_tabCounter`. Теперь коллизии исключены в принципе.

### 4. Итог: 141 тест, чистый анализатор и настоящий комфорт
Результат проверили не только руками, но и строгими автоматическими тестами:
- Написали отдельный сценарий на открытие сразу пяти смешанных сессий (Terminal + SFTP для разных хостов), переключение в каталог, выборочное закрытие и возврат обратно.
- Добавили сквозной виджет-тест навигации в `app_smoke_test.dart`.
- Все **141 тест монорепозитория** горят зеленым.
- `flutter analyze` — идеальные **0 warnings, 0 errors**.
- Зарегистрировали инцидент `BUG-008` в трекере и закрыли его со статусом `VERIFIED`.

Теперь вкладками действительно приятно пользоваться: сколько бы серверов ты ни открыл, приложение не зажимает тебя в тиски, всё переключается плавно и надежно.

---

## Запись 09. Двухпанельный SFTP по-взрослому: меню по правому клику, загадка синей кнопки и матрица прав 0755

### 1. Повод: когда файлы качаются, но жить в этом неудобно
В прошлых записях мы прикрутили вкладку SFTP и научили её бодро гонять файлы туда-обратно. Казалось бы, базовый функционал работает, живи и радуйся. Но дьявол, как обычно, кроется в мелочах реальной эксплуатации.

Открываешь удаленный сервер — и руки по привычке тянутся к правой кнопке мыши: переименовать файл, выставить права доступа на скрипт (`chmod 0755`), быстро создать новую папку под проект или удалить старый лог. А правой кнопки нет: нажимаешь — пустота. Приходится лезть в терминал и руками набивать `chmod +x deploy.sh` или `mkdir build`.

Дальше — больше: выбрал файл на локальном диске, внизу вылезает какая-то загадочная пустая голубая плашка без единой надписи. Интуитивно догадываешься, что это кнопка выгрузки на сервер, но режет глаз невероятно. А когда файл наконец зальется — список на удаленной панели не шевелится, пока вручную не ткнешь иконку «Обновить».

В общем, стало очевидно: SFTP пора доводить до ума и делать из него нормальный инструмент уровня Termius или FileZilla.

### 2. Подводные камни: синий на синем и бутерброд из контекстных меню
Поставил задачу ИИ, сели разбираться. И тут по ходу пьесы вылезло сразу несколько интересных моментов:

1. **Кнопка-невидимка (синий текст на синем фоне):**
   Оказалось, в Flutter Material 3, если у `ElevatedButton` явно задать фон `accentBlue`, но забыть про цвет текста (`foregroundColor`), он берет дефолт из темы приложения. А дефолт там... тоже синий! Получалась идеальная мимикрия: кнопка есть, кликается, а надпись «Upload» и иконка стрелочки намертво сливаются с фоном. Поправили одной строкой: принудительно задали белый цвет.

2. **Эффект бутерброда при правом клике:**
   Сделали контекстное меню на правый клик. Проверяем: кликаешь по конкретному файлу — и на экране мгновенно открываются сразу два меню друг поверх друга! Одно меню файла (Скачать, Chmod, Удалить), а прямо под ним торчит фоновое меню каталога (Создать папку, Создать файл).
   Причина классическая для Flutter: жест `onSecondaryTapDown` обрабатывался сначала виджетом файла, а затем всплывал выше и триггерил родительский жест всего списка. Добавили аккуратный флаг `_itemRightClickHandled`, который прерывает всплытие, если кликнули по конкретному объекту.

3. **Ловушка полного каталога:**
   Тут же возник закономерный вопрос: вот сделали мы вызов меню создания папки по правому клику на пустое место. А что, если в папке 50 файлов и экран забит до самого низа? Куда кликать, если свободного места тупо нет?
   Решили вопрос сразу с двух сторон:
   - В верхнюю панель инструментов рядом с обновлением вывели две явные кнопки: `[📁+ New Directory]` и `[📄+ New File]`. Теперь создать что угодно можно в один клик независимо от того, где ты находишься в списке.
   - А самому списку добавили постоянный отступ снизу в 80 пикселей. Теперь под последним файлом всегда есть чистое пространство для клика мышью.

4. **Интерактивный Chmod без зубрежки восьмеричных масок:**
   Права файлов в Linux — отдельная песня. Кто-то наизусть помнит `0755` и `0644`, а кому-то проще галочками отметить «чтение/запись/выполнение» для владельца, группы и остальных.
   Сделали красивый диалог: матрица чекбоксов 3x3, связанная с полем ввода маски. Меняешь галочки — маска пересчитывается на лету. Вбиваешь руками `777` — галочки сами мгновенно расставляются.

5. **Автообновление в реальном времени:**
   Привязали метод `reload()` панелей к завершению передачи (`onDone`). Залил файл или скачал лог — список в панели сам перечитывается в ту же секунду, без ручного кликания по кнопкам.

### 3. Итог: 147 тестов и новый Windows Release
- В контракты `ISftpSession` добавили методы `setPermissions` и `createFile`, поддержали их в сетевом ядре через `dartssh2`.
- Написали набор тестов на расчет прав доступа, диалоги и регрессию кликов (`sftp_dialogs_test.dart`).
- Зарегистрировали и закрыли дефекты `BUG-009` и `BUG-010` в нашем баг-трекере.
- Все 147 тестов монорепозитория проходят на ура, `flutter analyze` молчит.
- Собрали свежий релизный exe-файл под Windows.

Теперь с файлами на сервере можно работать комфортно: всё под рукой, ничего не наслаивается, а встроенный редактор конфигов спокойно вынесли в следующую фазу, как и планировали.

---

## Запись 10. Иконки ОС как в Termius: никакого ручного выбора, честное автоопределение по SSH и сочные векторные SVG

### 1. Повод: почему выбирать ОС руками в 2026 году — это прошлый век
Смотришь на карточки серверов в Termius — и глаз радуется: сразу видишь яркую оранжевую круговую эмблему Ubuntu, малиновую спираль Debian, суровые горы Alpine, геометрический ромб Arch или флаг Windows. Серверы считываются мгновенно, без вчитывания в длинные домены и IP-адреса.
У нас в карточках до сегодняшнего дня висели временные Material-иконки (бублик вместо Дебиана, бесконечность вместо Убунту), а в диалоге создания сервера торчал скучный выпадающий список.
И тут родилась главная мысль: **зачем вообще заставлять человека руками указывать операционную систему?** Машина подключается к серверу по SSH и может узнать всё сама за полсекунды. Никаких селекторов. Создал хост с нейтральной иконкой сервера, нажал «Connect» — и система сама определила дистрибутив и навсегда раскрасила карточку фирменным логотипом.

### 2. Подводные камни: мультиплексирование SSH-каналов и парсинг семейств
Чтобы автоопределение работало идеально и незаметно:
1. **Терминал не должен ждать и засоряться лишними символами:**
   Мы использовали мощь протокола SSH и библиотеки `dartssh2`: одно соединение умеет мультиплексировать несколько независимых каналов (channels). Интерактивный PTY-шелл открывается мгновенно, а параллельно в фоне запускается отдельный exec-канал с легким запросом:
   `cat /etc/os-release 2>/dev/null || uname -s 2>/dev/null`
2. **Ловушка производных дистрибутивов (`ID_LIKE`):**
   В первых тестах Rocky Linux и AlmaLinux внезапно определились как Fedora. Почему? Потому что в их `/etc/os-release` прописано родство: `ID_LIKE="rhel centos fedora"`. Если парсить простым поиском подстроки, Fedora перехватывала управление. Мы переписали парсер: сначала регулярным выражением вытаскивается точный ключ `ID=...`, и только если его нет — включается полнотекстовый фолбэк.
3. **Честный вектор вместо пиксельных картинок:**
   Подключили `flutter_svg: ^2.0.17` и заложили точные векторные SVG-логотипы (Ubuntu, Debian, Alpine, Arch, Fedora, CentOS, Rocky, AlmaLinux, Red Hat, FreeBSD, Raspberry Pi, macOS, Windows, Router/OpenWrt, Generic Server) с фирменными цветами подложки и мягким свечением.

### 3. Итог: 151 тест, 0 ошибок анализатора, чистый UX и свежий Windows Release
- Ручной селектор ОС из диалога создания хоста полностью удален: пользователь заполняет только адрес и креды.
- При первом подключении к хосту (терминал или SFTP) фоновый `OsDetector` распознает ОС, сохраняет ее в зашифрованную SQLite-базу и реактивно обновляет UI.
- Все карточки серверов (в сетке Grid и компактном списке Dense List) теперь отображают сочные векторные SVG-значки.
- Написано 14 специализированных тестов на парсинг реальных дистрибутивов, общее число тестов выросло до **151 (все 100% зеленые)**, `flutter analyze` кристально чист.
- **Собрали свежий Windows Release:** применили Safe Build Protocol (перенос буферов сборщика на `D:\temp`), компиляция C++/Flutter завершилась ровно за 30 секунд без малейшей нагрузки на железо. Свежий исполняемый файл `shellit.exe` и AOT-бинарник `app.so` (9.5 МБ) со встроенным SVG-движком готовы к боевой эксплуатации по адресу `apps/shellit/build/windows/x64/runner/Release/`.

---

## Запись 11. Черный ящик для серверов: живой пульт логов, авторотация на диске и asciinema-запись сессий

### 1. Повод: когда в сайдбаре висит заглушка, а в проде случается сбой
Честно скажу: я синтаксиса языков не знаю, в скобочках и ключевых словах путаюсь — мозг как-то не так устроен, видимо ))) Зато как пользователь и архитектор я прекрасно знаю, что такое внезапный сбой на сервере в полночь. 

Всё это время в сайдбаре Shellit сиротливо висела вкладка `Logs`. Кликаешь по ней — а там статичная серая заглушка: *«All passwords and sensitive tokens are masked by AppLogger»*. В отладчик Dart что-то, конечно, сыпалось, но для реальной работы это пшик. Если соединение с сервером оборвалось, SFTP споткнулся на странных правах файла или база выдала ошибку — где смотреть, что стряслось? Не полезешь же в консоль разработчика.

А главное — терминальные сессии. Сколько раз бывало: починил что-то на сервере сложной цепочкой из пяти команд с пайпами, закрыл терминал... и через два дня судорожно пытаешься вспомнить: *«А какой флаг я там передавал в awk?»*. Нужен был полноценный «черный ящик»: и для здоровья самого приложения, и для истории того, что происходило в консолях.

### 2. Подводные камни: утечки паролей, диск не резиновый и магия asciinema
Когда я сел продумывать задачу вместе с ИИ, мы сразу наткнулись на несколько фундаментальных развилок:

1. **Главный закон безопасности — ноль утечек:**
   Логировать всё подряд нельзя ни в коем случае. Если в лог случайно попадет приватный ключ SSH или пароль от root, вся безопасность летит в трубу. Поэтому все сообщения без исключений обязаны прогоняться через жесткую санитизацию: приватные ключи заменяются на `***[REDACTED_PRIVATE_KEY]***`, а любые параметры вида `password=...` или `token=...` надежно маскируются звездочками.
2. **Диск не резиновый (авторотация):**
   Если просто бездумно писать логи в файл, рано или поздно программа сожрет всё свободное место. Сделали строгую авторотацию: основной файл `shellit.log` и один архивный `shellit.1.log`. Лимит — ровно 5 МБ на файл. Достиг порога — старый лог ротируется, новый начинает писаться с чистого листа. Итого максимум 10 МБ на диске в папке приложения, ни байтом больше.
3. **Запись видео сессий без гигабайтов видеофайлов:**
   Записывать экран терминала в MP4 или GIF — безумие, диск заполнится за пару часов. В мире Linux есть золотой стандарт — **asciinema v2 (`.cast`)**. Это компактный текстовый JSON-поток, где сохраняются только символы и точные задержки времени: `[0.125, "o", "ls -la\n"]`. Запись 20-минутной активной работы весит всего 150–300 килобайт! А рядом с ней параллельно формируется чистый текстовый `.log`, чтобы по нему можно было мгновенно искать слова через обычный поиск.
4. **Коварный спиннер в тестах Flutter:**
   По ходу пьесы выловили забавный баг в виджет-тестах: метод `tester.pumpAndSettle()` намертво зависал и падал по таймауту. Оказалось, стандартный `CircularProgressIndicator` крутит бесконечную анимацию, и Flutter Test наивно ждет, пока «все кадры анимации на экране закончатся». Заменили ожидание на порционный `pump` с дельтой времени — и тесты побежали как по маслу.

### 3. Логика решений с ИИ: терминальный пульт управления в два таба
Разложили архитектуру по полочкам:

- **Ядро `core_foundation`:** Создали строгую модель `LogEntry` и прикрутили широковещательный поток `AppLogger.entryStream`. Теперь любое событие в сети, криптографии или UI мгновенно летит подписчикам в очищенном от паролей виде.
- **Движок записи в `ssh_network_core`:** Реализовали класс `SessionRecorder`. Он подключается прямо к PTY-потокам терминала, считает введенные команды (по нажатиям Enter) и без малейших задержек для пользователя пишет события в `.cast` и `.log`.
- **Службы в `apps/shellit`:** 
  - `FileLogSink` отвечает за ротацию файлов на диске;
  - `SessionStorageService` ведет индекс всех завершенных сессий в JSON;
  - `SystemLogsController` держит в оперативной памяти кольцевой буфер на 1500 событий для сверхбыстрого отклика интерфейса.
- **Экран `AuditLogsScreen`:** 
  В сайдбаре вместо заглушки теперь открывается полноценный центр управления:
  - **Вкладка System Logs:** цветные чипы уровней (`ALL`, `DEBUG`, `INFO`, `WARN`, `ERROR`), живой поиск по тексту и тегам, автоскролл за новыми событиями, кнопка паузы стрима, копирование в буфер и всплывающее окно по клику на любую строчку — с полным текстом ошибки и стектрейсом.
  - **Вкладка Session Recordings:** карточки всех сессий с бейджами окружений (`PROD`, `STAGE`, `DEV`), длительностью сессии, количеством команд и встроенным просмотрщиком текста вывода.
- **Настройки в Settings:** 
  Добавили блок «Logs & Diagnostics»: можно включить или выключить запись на диск, выбрать минимальный уровень логирования, в один клик открыть папку с логами в проводнике Windows (`explorer.exe`) или нажать кнопку полной очистки дисковых логов.

### 4. Итог: 157 тестов и абсолютный контроль
- Написали набор тестов `system_logs_screen_test.dart` и `session_recorder_test.dart` — проверили буферизацию, фильтры, asciinema-заголовки и ротацию файлов.
- Все **157 тестов монорепозитория** горят зеленым.
- Статический анализатор `flutter analyze` выдает кристальные **0 warnings, 0 errors**.
- Теперь Shellit ничего не скрывает: любая сетевая ошибка, подключение или команда видны как на ладони, а пароли и закрытые ключи остаются под замком.


